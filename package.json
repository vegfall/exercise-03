{
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "check": "prettier --check .",
    "prettier:update": "prettier --write ."
  }
}
